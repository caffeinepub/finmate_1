{
  "frontend/src/components/ScanPayModal.tsx": "Scan & Pay modal with two tabs — QR code scanner (using relative import path `../qr-code/useQRScanner`) and phone number payment entry with amount field and pay button.",
  "frontend/src/pages/Home.tsx": "Home page with a fixed balance card embedded in the header that requires PIN or fingerprint authentication (via `PinConfirmationModal`) before revealing the total balance, with auto-hide after 30 seconds; the \"Check Balance\" quick action also triggers the same PIN gate.",
  "frontend/src/pages/History.tsx": "Transaction history page with search, time period filter (All/Weekly/Monthly/Yearly), type filter (All/Debit/Credit), category chip filters, summary totals, and a sorted list of TransactionCard components.",
  "frontend/src/pages/Analytics.tsx": "Analytics page with period selector, demo summary stats, Recharts PieChart and BarChart with mock data, category breakdown bars, and a fully embedded AI chatbot panel — no BottomNav rendered here (BottomNav is managed by MainApp).",
  "frontend/src/pages/Profile.tsx": "Profile page with all sections (balance, FinMate wallet, bank accounts, cards, referral, leaderboard prefs, settings), using correct prop names `isOpen` for `AddBankAccountModal` and `open` for `SecuritySettingsModal`, and removing the non-existent `onLock` prop from `SecuritySettingsModal`.",
  "frontend/src/index.css": "Global styles with OKLCH color system for light/dark modes and FinMate purple-blue theme — all `@layer components` rules now use plain CSS properties instead of `@apply` with opacity modifiers or variant utilities, which were causing PostCSS build failures.",
  "frontend/src/hooks/usePinAuth.ts": "PIN authentication hook with both new API (isPinSet, setPin, verifyPin, biometricEnabled, setBiometricEnabled) and legacy-compatible API (setupPin, simulateBiometric, state, error, clearError) to support all existing consumers.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks for all backend operations, plus all helper exports (getCategoryLabel, getCategoryEmoji, formatCurrency, formatDate, formatTime, getDateRangeForPeriod, CATEGORY_COLORS) needed by other components.",
  "frontend/src/App.tsx": "Main app component that handles authentication flow using the updated usePinAuth hook with isPinSet/setupPin/verifyPin/simulateBiometric API, managing PIN setup, PIN entry, and main app rendering.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal for new users, now includes `walletBalance: BigInt(0)` in the profile object passed to `saveProfile` to satisfy the updated `UserProfile` type.",
  "frontend/src/hooks/useAIChatbot.ts": "Rule-based AI chatbot hook with zero-argument signature, using ChatMessage type with role 'user'|'assistant' and content field, covering spending, investment, budget, goals, credit, and income topics.",
  "frontend/src/components/AIChatbotOverlay.tsx": "AI chatbot overlay component using index-based keys for messages (no `id` field on ChatMessage), with quick-reply shortcuts and a scrollable message thread.",
  "frontend/src/components/AIChatbotFAB.tsx": "Floating action button that opens the AIChatbotOverlay without passing transactions, since the chatbot hook now uses demo data internally.",
  "frontend/src/components/CustomerFeedbackSection.tsx": "Customer feedback section with 1–5 star rating and review textarea, correctly casting the numeric rating to `bigint` before calling the backend mutation.",
  "frontend/src/components/BalanceCard.tsx": "PIN-protected balance card component that shows total balance across linked accounts, now using the Wallet icon (lucide-react) instead of the custom image asset for the Check Balance button, with auto-hide after 30 seconds.",
  "frontend/src/lib/qrcode.ts": "Pure TypeScript QR code matrix generator with no external dependencies — implements Reed-Solomon error correction and QR code encoding (byte mode, version 1-10, EC level L) to produce a 2D boolean matrix.",
  "frontend/src/components/WalletQRModal.tsx": "Modal dialog that displays a user's unique FinMate Wallet QR code (generated client-side from their principal/wallet ID), with copy and share actions, styled with the FinMate purple-blue gradient theme.",
  "frontend/src/components/FinMateWalletCard.tsx": "Reusable FinMate Wallet card component showing wallet balance, short wallet ID, and Send/Receive/QR action buttons with a purple-blue gradient design; opens WalletQRModal for receiving and ScanPayModal for sending.",
  "frontend/src/components/PinConfirmationModal.tsx": "PIN confirmation modal with a numeric keypad and a fingerprint/biometric authentication button (bottom-left of keypad); calls `simulateBiometric` from `usePinAuth` on fingerprint tap and invokes `onSuccess` on either successful PIN entry or biometric auth."
}